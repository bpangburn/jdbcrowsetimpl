<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
                             
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.nqadmin.rowset</groupId>
    <artifactId>jdbcrowsetimpl</artifactId>
    <version>1.0.5-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>jdbcrowsetimpl</name>
    <description>Java 8-compatible, repackaged OpenJDK JdbcRowSet implementation based on the fixed OpenJDK jdk-25.0.1-ga source tag.</description>
    <url>https://github.com/bpangburn/jdbcrowsetimpl</url>
    <inceptionYear>2020</inceptionYear>

    <organization>
        <name>The Pangburn Group, Inc.</name>
        <url>https://pangburngroup.com</url>
    </organization>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!--
            Source baseline:
            - OpenJDK repository: https://github.com/openjdk/jdk25u
            - Fixed source tag: jdk-25.0.1-ga
            - OpenJDK changeset shown for the tag: 78770bfa

            Compatibility policy:
            - The artifact remains Java 8 compatible.
            - Source files are repackaged from com.sun.rowset to com.nqadmin.rowset.
            - Newer OpenJDK implementation details are adapted or omitted when they require
              Java APIs unavailable on Java 8.
        -->
        <openjdk.source.tag>jdk-25.0.1-ga</openjdk.source.tag>
        <openjdk.source.changeset>78770bfa</openjdk.source.changeset>

        <!--
            Release-safe defaults:
            - Maven 3.9.2+ is enforced because the Central Publishing Maven Plugin
              uses Maven Plugin API 3.9.2.
            - The normal project build remains Java 8 compatible.
            - OWASP Dependency-Check 12.x is kept in an opt-in profile and requires
              running Maven with JDK 11+.
            - Avoid Maven 4.x beta/RC plugin lines until the project intentionally
              moves to Maven 4.
        -->
        <version.maven>[3.9.2,)</version.maven>
        <version.java.release>8</version.java.release>
        <version.java.minimum>[1.8,)</version.java.minimum>
        <version.owasp.java.minimum>[11,)</version.owasp.java.minimum>

        <version.maven-enforcer-plugin>3.6.3</version.maven-enforcer-plugin>
        <version.maven-compiler-plugin>3.15.0</version.maven-compiler-plugin>
        <version.dependency-check-maven>12.2.2</version.dependency-check-maven>
        <version.maven-source-plugin>3.4.0</version.maven-source-plugin>
        <version.maven-javadoc-plugin>3.12.0</version.maven-javadoc-plugin>
        <version.maven-gpg-plugin>3.2.8</version.maven-gpg-plugin>
        <version.central-publishing-maven-plugin>0.10.0</version.central-publishing-maven-plugin>
    </properties>

    <licenses>
        <license>
            <name>GNU General Public License, version 2, with the Classpath Exception</name>
            <url>https://spdx.org/licenses/GPL-2.0-with-classpath-exception.html</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Brian E. Pangburn</name>
            <email>swingset@pangburngroup.com</email>
            <organization>The Pangburn Group, Inc.</organization>
            <organizationUrl>https://pangburngroup.com</organizationUrl>
        </developer>
        <developer>
            <name>Prasanth R. Pasala</name>
            <email>swingset@pangburngroup.com</email>
            <organization>The Pangburn Group, Inc.</organization>
            <organizationUrl>https://pangburngroup.com</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/bpangburn/jdbcrowsetimpl.git</connection>
        <developerConnection>scm:git:git@github.com:bpangburn/jdbcrowsetimpl.git</developerConnection>
        <url>https://github.com/bpangburn/jdbcrowsetimpl</url>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/bpangburn/jdbcrowsetimpl/issues</url>
    </issueManagement>

    <distributionManagement>
        <!--
            Central Portal supports SNAPSHOT publishing. Keep this only if your
            Sonatype Central namespace/token is configured for snapshots.
        -->
        <snapshotRepository>
            <id>central</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${version.maven-enforcer-plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${version.maven-compiler-plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.owasp</groupId>
                    <artifactId>dependency-check-maven</artifactId>
                    <version>${version.dependency-check-maven}</version>
                    <configuration>
                        <failBuildOnCVSS>7.0</failBuildOnCVSS>
					    <ossIndexServerId>sonatype-guide</ossIndexServerId>
					    <ossIndexAnalyzerUrl>https://api.guide.sonatype.com</ossIndexAnalyzerUrl>
					    <!--
						<nvdApiServerId>nvd</nvdApiServerId>
						<ossIndexServerId>ossindex</ossIndexServerId>
						-->
					</configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${version.maven-source-plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${version.maven-javadoc-plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${version.maven-gpg-plugin}</version>
                </plugin>
                <plugin>
                    <groupId>org.sonatype.central</groupId>
                    <artifactId>central-publishing-maven-plugin</artifactId>
                    <version>${version.central-publishing-maven-plugin}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>enforce-build-baseline</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>${version.maven}</version>
                                    <message>This project requires Maven 3.9.2 or newer. Maven 3.9.16 is recommended.</message>
                                </requireMavenVersion>
                                <requireJavaVersion>
                                    <version>${version.java.minimum}</version>
                                    <message>The normal project build requires Java 8 or newer.</message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${version.java.release}</release>
                    <showWarnings>true</showWarnings>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>owasp-check</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-enforcer-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>enforce-owasp-java-baseline</id>
                                <goals>
                                    <goal>enforce</goal>
                                </goals>
                                <configuration>
                                    <rules>
                                        <requireJavaVersion>
                                            <version>${version.owasp.java.minimum}</version>
                                            <message>OWASP Dependency-Check ${version.dependency-check-maven} requires Maven to run on JDK 11 or newer.</message>
                                        </requireJavaVersion>
                                    </rules>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <executions>
                            <execution>
                                <id>dependency-check</id>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <failBuildOnCVSS>7.0</failBuildOnCVSS>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <configuration>
                            <source>${version.java.release}</source>
                            <doclint>none</doclint>
                            <failOnError>false</failOnError>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <!--
                                        Expected settings.xml entry:
                                        <server>
                                            <id>${gpg.keyname}</id>
                                            <passphrase>...</passphrase>
                                        </server>
                                    -->
                                    <keyname>${gpg.keyname}</keyname>
                                    <passphraseServerId>${gpg.keyname}</passphraseServerId>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <extensions>true</extensions>
                        <configuration>
                            <!--
                                Expected settings.xml entry:
                                <server>
                                    <id>central</id>
                                    <username>central-portal-token-username</username>
                                    <password>central-portal-token-password</password>
                                </server>
                            -->
                            <publishingServerId>central</publishingServerId>

                            <!-- Keep false for manual review/publish in Central Portal. -->
                            <autoPublish>false</autoPublish>
                            <waitUntil>validated</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
